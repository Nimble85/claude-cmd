# Claude Code + Microsoft Foundry Configuration for Windows (TEMPLATE)
# This is a template file - copy to intro.ps1 and edit with your credentials
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: Copy-Item intro.ps1.template intro.ps1
# 2. Edit intro.ps1 with your Azure Foundry deployment details
# 3. Run in PowerShell: .\intro.ps1
# 4. Start Claude Code: claude (or use .\start-claude.ps1)
###############################################################################

# Your Azure Foundry Configuration
$env:ANTHROPIC_BASE_URL = "https://YOUR-RESOURCE.services.ai.azure.com/anthropic/"
$env:ANTHROPIC_API_KEY = "YOUR-API-KEY-HERE"
$env:ANTHROPIC_MODEL = "claude-opus-4-5"

# Optional: Additional model aliases
$env:ANTHROPIC_DEFAULT_OPUS_MODEL = "claude-opus-4-5"
$env:ANTHROPIC_DEFAULT_SONNET_MODEL = "claude-sonnet-4-5"
$env:ANTHROPIC_DEFAULT_HAIKU_MODEL = "claude-haiku-4-5"

# Confirmation message
Write-Host "✅ Claude Code configured for Microsoft Foundry" -ForegroundColor Green
Write-Host "   Endpoint: $env:ANTHROPIC_BASE_URL"
Write-Host "   Model: $env:ANTHROPIC_MODEL"
Write-Host "   API Key: $($env:ANTHROPIC_API_KEY.Substring(0, [Math]::Min(20, $env:ANTHROPIC_API_KEY.Length)))..."
Write-Host ""
Write-Host "🚀 Start Claude Code with: .\start-claude.ps1" -ForegroundColor Cyan

